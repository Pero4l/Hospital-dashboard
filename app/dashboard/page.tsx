import Layout from '@/app/components/Layout';
import UserTable from '@/app/components/UserTable';

const Home = () => {
  return (
    <Layout>
      <UserTable />
    </Layout>
  );
};

export default Home;
